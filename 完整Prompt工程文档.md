**完整Prompt工程文档**

语义识别与图表推荐模块

# **一、文档目标**

本文档用于记录本项目语义识别模块的Prompt工程设计过程，包括模块目标、输入输出规范、意图标签体系、Prompt迭代过程、RAG增强策略、边缘案例处理规则、最终Prompt模板和维护建议。通过形成完整Prompt工程文档，可以保证后续成员在维护语义识别模块时能够理解Prompt设计依据，并能够在新增场景时持续迭代。

# **二、模块定位**

语义识别模块位于PPT解析模块之后、图表生成模块之前，主要负责将PPT页面中的文本、表格内容和用户补充指令转换为结构化图表生成需求。模块输入包括页面文本、表格字段、用户选择的图表偏好和历史案例检索结果；模块输出为固定JSON结构，供后续图表生成与配图模块调用。

# **三、意图标签体系**

| **标签**     | **定义**   | **判定依据**                             | **推荐图表**         |
|--------------|------------|------------------------------------------|----------------------|
| comparison   | 比较类意图 | 出现“高于、低于、对比、差异、排名”等表达 | 柱状图、条形图       |
| trend        | 趋势类意图 | 存在时间维度，关注增长、下降、波动或变化 | 折线图、面积图       |
| composition  | 构成类意图 | 关注整体与部分之间的比例、占比和组成     | 饼图、环形图、堆叠图 |
| distribution | 分布类意图 | 关注数值在区间、等级或类别中的分布情况   | 直方图、箱线图       |
| correlation  | 相关类意图 | 关注两个变量之间的关系、联动或影响       | 散点图、气泡图       |
| unknown      | 不确定意图 | 信息不足或用户需求不明确                 | 返回澄清提示         |

# **四、Prompt版本演化**

| **版本**          | **主要目标**       | **改进内容**                                | **局限性**                 |
|-------------------|--------------------|---------------------------------------------|----------------------------|
| V1 基础版         | 完成基础语义识别   | 要求模型判断意图并给出图表类型              | 输出不稳定，JSON格式不统一 |
| V2 结构约束版     | 保证输出可解析     | 加入固定JSON字段和枚举标签                  | 边缘案例识别能力不足       |
| V3 规则增强版     | 提升分类准确率     | 补充五类意图定义、关键词规则和图表推荐规则  | 对多意图文本处理不充分     |
| V4 RAG增强版      | 利用案例库辅助判断 | 引入相似案例检索和few-shot示例              | 案例库规模有限             |
| V5 边缘规则完善版 | 增强鲁棒性         | 增加多意图优先级、unknown降级和澄清提示规则 | 需要持续根据真实样本更新   |

# **五、最终输出JSON规范**

最终Prompt要求模型必须输出合法JSON，不允许输出解释性自然语言。JSON字段如下：

| **字段**               | **类型**    | **说明**                                                                                   |
|------------------------|-------------|--------------------------------------------------------------------------------------------|
| intent                 | string      | 主意图标签，只能从comparison、trend、composition、distribution、correlation、unknown中选择 |
| secondary_intent       | string/null | 当存在明显多意图时记录次要意图，无次要意图时为null                                         |
| chart_type             | string      | 推荐图表类型，例如bar、line、pie、scatter等                                                |
| reason                 | string      | 简要说明选择该意图和图表类型的依据                                                         |
| confidence             | number      | 0到1之间的置信度                                                                           |
| need_clarification     | boolean     | 是否需要用户进一步补充信息                                                                 |
| clarification_question | string/null | 需要澄清时给出的追问                                                                       |

# **六、最终Prompt模板**

你是一个PPT图表语义分析助手。你的任务是根据输入的PPT页面文本、表格内容和用户指令，判断该页面最适合生成的图表意图和图表类型。  
  
请严格遵守以下规则：  
1. 只输出JSON，不输出额外解释。  
2.
intent只能从comparison、trend、composition、distribution、correlation、unknown中选择。  
3. 若文本包含时间维度并强调变化，优先判断为trend。  
4. 若文本强调不同对象之间的高低、差异、排名，优先判断为comparison。  
5. 若文本强调整体中的比例、份额、组成，优先判断为composition。  
6. 若文本强调区间、集中程度、离散程度，优先判断为distribution。  
7.
若文本强调两个变量之间的关系、影响、同步变化，优先判断为correlation。  
8. 若输入不足以判断，请输出unknown，并将need_clarification设为true。  
9.
若存在多个意图，请选择最符合用户分析目标的主意图，并在secondary_intent中记录次要意图。  
  
输出字段：intent、secondary_intent、chart_type、reason、confidence、need_clarification、clarification_question。

# **七、RAG增强策略**

RAG增强主要用于边缘案例和相似表达处理。系统将历史标注样本、典型错误案例和人工修正案例整理为案例库。当新输入进入语义识别模块时，系统先检索相似案例，将最相近的若干条案例作为few-shot示例注入Prompt，从而提高模型对模糊表达和多意图文本的判断一致性。

| **案例库字段**  | **说明**                   |
|-----------------|----------------------------|
| case_id         | 案例编号                   |
| input_text      | 原始输入文本               |
| expected_intent | 人工标注意图               |
| chart_type      | 推荐图表类型               |
| error_type      | 若为错误案例，记录错误类型 |
| fix_rule        | 人工总结的修正规则         |

# **八、边缘案例处理规则**

> •
> 多意图文本：优先判断用户主要分析目标，并将次要意图写入secondary_intent。
>
> • 短文本或弱指令：若无法判断具体图表目标，返回unknown并给出澄清问题。
>
> • 否定表达：识别“不是……而是……”后的真实需求，避免被前半句误导。
>
> •
> 时间与占比同时出现：若关注占比随时间变化，可推荐堆叠柱状图或面积图，并记录双意图。
>
> •
> 相关与比较混合：若用户关注两个变量是否联动，优先correlation；若关注不同对象高低，优先comparison。

# **九、维护建议**

后续维护中应持续收集真实PPT页面样本，将误判案例加入RAG案例库，并定期复查Prompt规则是否与图表生成模块的能力保持一致。若新增图表类型或业务场景，应同步更新意图定义、输出JSON字段、few-shot案例和测试集。
